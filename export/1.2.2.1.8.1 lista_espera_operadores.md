---
id: 1.2.2.1.8.1
title: Lista de Espera Operadores
parent: 1.2.2.1.8
breadcrumb: 0 → 1.0 → 1.2 → 1.2.2 → 1.2.2.1 → 1.2.2.1.8 → 1.2.2.1.8.1
type: hoja
status: activo
last_updated: 2026-01-29 15:32
file_create: 2025-12-27 00:34
domain:
- operador
- lista_espera
- ciudades
- expansion
summary: 'Sistema para capturar interés de operadores en ciudades nuevas.

  Posición en lista = prioridad cuando ciudad se active.

  '
content_hash: 34e55619c95734cb
---

# 1.2.2.1.8.1 Lista de Espera Operadores

> Sistema para capturar interés de operadores en ciudades nuevas.

---

## Modelo de Datos

```sql
CREATE TABLE operadores_interesados (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  
  -- Datos del prospecto
  nombre VARCHAR(100) NOT NULL,
  apellidos VARCHAR(100),
  telefono VARCHAR(20) NOT NULL,
  email VARCHAR(255) NOT NULL,
  
  -- Ciudad solicitada
  ciudad_nombre VARCHAR(100) NOT NULL,
  estado_nombre VARCHAR(100) NOT NULL,
  ciudad_id UUID REFERENCES ciudades(id),  -- Si existe
  
  -- Estado
  estado VARCHAR(20) DEFAULT 'esperando',  -- esperando, notificado, registrado, expirado
  
  -- Prioridad (orden de llegada)
  posicion INT,
  
  -- Notificación
  notificado_at TIMESTAMPTZ,
  
  created_at TIMESTAMPTZ DEFAULT now()
);

-- Índice para contar por ciudad
CREATE INDEX idx_operadores_int_ciudad ON operadores_interesados(ciudad_nombre, estado_nombre);
```

---

## UI: Registro de Interés

```
┌─────────────────────────────────────────────────────────────────┐
│  📍 MONTERREY, NUEVO LEÓN                                       │
│     Aún no disponible                                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ¡Pero estamos creciendo! Déjanos tus datos y te avisaremos     │
│  cuando lleguemos a tu ciudad.                                  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                                                         │    │
│  │  Nombre *                                               │    │
│  │  ┌─────────────────────────────────────────────────┐    │    │
│  │  │ Juan Carlos                                     │    │    │
│  │  └─────────────────────────────────────────────────┘    │    │
│  │                                                         │    │
│  │  Teléfono *                                             │    │
│  │  ┌─────────────────────────────────────────────────┐    │    │
│  │  │ 81 1234 5678                                    │    │    │
│  │  └─────────────────────────────────────────────────┘    │    │
│  │                                                         │    │
│  │  Email *                                                │    │
│  │  ┌─────────────────────────────────────────────────┐    │    │
│  │  │ juan@email.com                                  │    │    │
│  │  └─────────────────────────────────────────────────┘    │    │
│  │                                                         │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  🎁 BENEFICIO: Serás de los primeros operadores                 │
│     y tendrás prioridad en asignación de servicios.             │
│                                                                 │
│  ℹ️ Ya hay 8 operadores interesados en Monterrey                │
│                                                                 │
│  [ Avisarme cuando esté disponible ]                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## UI: Confirmación

```
┌─────────────────────────────────────────────────────────────────┐
│  ✅ ¡REGISTRADO!                                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Te avisaremos cuando OnlyCar llegue a Monterrey.               │
│                                                                 │
│  Tu posición en la lista: #9                                    │
│                                                                 │
│  Recibirás:                                                     │
│  • Email cuando la ciudad se active                             │
│  • Link para continuar tu registro                              │
│  • Prioridad por ser de los primeros                            │
│                                                                 │
│  [ Volver al inicio ]                                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Lógica de Registro

```typescript
const registrarInteres = async (datos: {
  nombre: string;
  telefono: string;
  email: string;
  ciudad: string;
  estado: string;
}) => {
  // Contar posición
  const { count } = await supabase
    .from('operadores_interesados')
    .select('*', { count: 'exact' })
    .eq('ciudad_nombre', datos.ciudad)
    .eq('estado_nombre', datos.estado);
  
  const posicion = (count || 0) + 1;
  
  // Registrar
  await supabase.from('operadores_interesados').insert({
    ...datos,
    posicion
  });
  
  return { posicion };
};
```

---

## Navegación

| ⬆️ Padre             | [[Proyecto OnlyCarNLD/Datos/1.2.2.1.8 ciudad_no_disponible]]     |
| -------------------- | -------------------------------------- |
| ➡️ Hermano siguiente | [[Proyecto OnlyCarNLD/Datos/1.2.2.1.8.2 notificacion_activacion]]|

---
